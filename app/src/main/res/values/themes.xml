<resources>

    <style name="Theme.BrowseHomes" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryVariant">@android:color/white</item>
        <item name="colorOnPrimary">@color/flush</item>
    </style>

    <style name="Theme.BrowseHomes.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.BrowseHomes.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BrowseHomes.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>